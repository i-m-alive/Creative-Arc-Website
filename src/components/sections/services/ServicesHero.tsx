import styles from "./ServicesHero.module.css";

export default function ServicesHero() {
  return (
    <section className={styles.hero}>
      <h1>AI Services</h1>
      <p>
        From strategy to deployment, we help organizations adopt AI with
        confidence.
      </p>
    </section>
  );
}
